const mongoose = require("mongoose");
const {
    format_json
} = require("../helper");
const {
    userModel
} = require("../models/UserModel");




module.exports.authGoogle = async (req, res) => {

}